[project]
name = "arxivembedding"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.4.1",
    "polars>=1.34.0",
    "pyarrow>=22.0.0",
]
